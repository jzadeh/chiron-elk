import logging

log = logging.getLogger(__name__)

class nmap_analytics:
"""Stats logic for nmap data

  Attributes:
  nmap_dataframe:
  """

    def __init__(self, directory_to_parse, move_files_after_parsing = true):
        self.directory = directory_to_parse

    def add_top_n_stats(df1):

      
